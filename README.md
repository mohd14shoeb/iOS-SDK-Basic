# iOS SDK Basic

A minimal Swift package providing a basic SDK for iOS integration.

## Add package dependency

Add the following to your `Package.swift`:

```swift
.package(url: "https://github.com/USER/REPO.git", from: "1.0.0"),
```

Then add the target as a dependency.
